# Blender Source Files
These files are the raw `.blend` files to create all of the games 3D assets. 

These are not the GLB files actually used as those are contained elsewhere, specifically in the `assets/` directory.
